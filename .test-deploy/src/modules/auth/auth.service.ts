import { Injectable } from '@nestjs/common';

/**
 * Auth service — handles JWT token generation and validation.
 * TODO: Implement login, validateUser, generateToken, refreshToken
 */
@Injectable()
export class AuthService {
  // TODO: Inject JwtService, ConfigService, and user repository
}
