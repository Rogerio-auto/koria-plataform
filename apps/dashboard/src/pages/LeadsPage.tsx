/**
 * Leads page — list all leads with filters and search.
 * TODO: Implement with LeadsTable component + filter bar.
 */
export function LeadsPage() {
  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h2 className="text-2xl font-bold">Leads</h2>
        {/* TODO: Filter bar */}
      </div>
      <p className="text-muted-foreground">Leads table placeholder</p>
    </div>
  );
}
