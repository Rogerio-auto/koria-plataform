/**
 * Lead detail page — full info about a single lead.
 * TODO: Display lead info, contact points, qualification, conversations,
 *       work orders, payments, stage history, events timeline.
 */
export function LeadDetailPage() {
  return (
    <div className="space-y-6">
      <h2 className="text-2xl font-bold">Detalhes do Lead</h2>
      <p className="text-muted-foreground">Lead detail placeholder</p>
    </div>
  );
}
