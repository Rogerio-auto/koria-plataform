/**
 * Work orders table — displays work orders with status badges.
 * TODO: Implement with @koria/ui Table + Badge components.
 * Columns: ID, Lead, Product, Status, Due At, Upload Token, Actions
 */
export function WorkOrdersTable() {
  return (
    <div className="rounded-lg border">
      <p className="p-4 text-sm text-muted-foreground">Work orders table placeholder</p>
    </div>
  );
}
