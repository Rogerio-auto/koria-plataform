/**
 * Leads table — displays leads with filtering, sorting, and pagination.
 * TODO: Implement with @koria/ui Table component + TanStack Query for data.
 * Columns: Name, Email, Status, Channel, Stage, Created At, Actions
 */
export function LeadsTable() {
  return (
    <div className="rounded-lg border">
      <p className="p-4 text-sm text-muted-foreground">Leads table placeholder</p>
    </div>
  );
}
